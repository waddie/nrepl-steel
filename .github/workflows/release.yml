name: Build and Release Dynamic Libraries

on:
  push:
    tags:
      - 'v*.*.*'
  # Manual dry-run: builds artifacts but skips the release/upload job.
  workflow_dispatch:

# Least-privilege default for all jobs; the release job widens this to
# `contents: write` for itself.
permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS — Apple Silicon (native on the arm64 runner)
          - os: macos-14
            target: aarch64-apple-darwin
            platform: aarch64-macos
            asset_name: libnrepl_steel_engine-aarch64-macos.dylib
            built_name: libnrepl_steel_engine.dylib

          # macOS — Intel (cross-compiled from the arm64 runner; the Apple
          # toolchain ships both arches, so no extra linker is needed)
          - os: macos-14
            target: x86_64-apple-darwin
            platform: x86_64-macos
            asset_name: libnrepl_steel_engine-x86_64-macos.dylib
            built_name: libnrepl_steel_engine.dylib

          # Linux — x86_64 (GNU)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: x86_64-linux
            asset_name: libnrepl_steel_engine-x86_64-linux.so
            built_name: libnrepl_steel_engine.so

          # Linux — aarch64 (GNU, native on the arm64 runner)
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            platform: aarch64-linux
            asset_name: libnrepl_steel_engine-aarch64-linux.so
            built_name: libnrepl_steel_engine.so

          # Windows — x86_64 (MSVC)
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: x86_64-windows
            asset_name: nrepl_steel_engine-x86_64-windows.dll
            built_name: nrepl_steel_engine.dll

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build nrepl-steel-engine cdylib
        run: cargo build --release --locked -p nrepl-steel-engine --target ${{ matrix.target }}

      - name: Rename artifact
        shell: bash
        run: |
          mv "target/${{ matrix.target }}/release/${{ matrix.built_name }}" "./${{ matrix.asset_name }}"

      - name: Upload artifact for release job
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.platform }}
          path: ./${{ matrix.asset_name }}

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    # Only publish a release for tag pushes (workflow_dispatch is a dry-run).
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Verify all version sources match the tag
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          # The version lives in three places; a release must not ship with any
          # of them stale (describe drifted to 0.2.0 while 0.2.4 was released).
          cog_version="$(grep -E '^\(define version ' cog.scm | sed -E 's/.*"([^"]+)".*/\1/')"
          scm_version="$(grep -E '^\(define nrepl-steel-version ' nrepl-server/version.scm | sed -E 's/.*"([^"]+)".*/\1/')"
          cargo_version="$(grep -E '^version = ' Cargo.toml | head -1 | sed -E 's/.*"([^"]+)".*/\1/')"
          fail=0
          for pair in "cog.scm:$cog_version" \
                      "nrepl-server/version.scm:$scm_version" \
                      "Cargo.toml:$cargo_version"; do
            file="${pair%%:*}"
            version="${pair#*:}"
            if [ "$tag" != "$version" ]; then
              echo "::error::tag v$tag does not match $file version '$version'" >&2
              fail=1
            else
              echo "$file version $version matches tag v$tag"
            fi
          done
          [ "$fail" -eq 0 ] || exit 1
          # Forge reads cog.scm as data (no evaluation), so the dylib URLs are
          # literal strings that must be bumped by hand. Guard against drift:
          # every release-download URL must carry the tag being released.
          stale="$(grep -oE 'releases/download/v[0-9][^/]*/' cog.scm | grep -v "releases/download/v$tag/" || true)"
          if [ -n "$stale" ]; then
            echo "::error::cog.scm has dylib URLs not pointing at v$tag:" >&2
            echo "$stale" >&2
            exit 1
          fi
          echo "all cog.scm dylib URLs point at v$tag"

      - name: Download all build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: List downloaded files
        run: ls -R artifacts

      - name: Create release and upload assets
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          files: artifacts/**/*
